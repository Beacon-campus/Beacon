export async function fetchSubjects(role) {
  // Real subjects should come from API.
  // Keep this helper neutral for now.
  void role;
  return [];
}

export async function fetchDocs(subject) {
  // Real uploads should come from API.
  void subject;
  return [];
}
